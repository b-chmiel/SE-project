import {VisitPriority, VisitStatus, VisitType} from '../api/visitAPI.types';

export const visitPriorities = Object.keys(VisitPriority);

export const visitTypes = Object.keys(VisitType);

export const visitStatuses = Object.keys(VisitStatus);
